REPOROOT = /path/to/git
REPO = $(REPOROOT)/sphpark

# install code on local filesytem under web root
WEBDIR = /path/to/sphpark 
LOGDIR = /path/to/sphark-log-directory
# URL on local server
LOCALURL = http://localhost/sphpark

# -- 
# identifier for the remote configuration;
#  idenitifes config_inc.<NAME>.php
REMOTENAME = 
# remote user specification,
REMOTE = johndoe@example.com
# full path to source code for auth handler on remote server
#  must correspond to redirect URL
REMOTEPATH =  /var/www/html/bots/mybot
# the URL corresponding 
REMOTEURL = 

